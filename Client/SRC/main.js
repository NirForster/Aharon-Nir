// imports script from controller
import controller from "./controller.js";
// when the HTML document has completely loaded - initialise
document.addEventListener("DOMContentLoaded", controller.init);
